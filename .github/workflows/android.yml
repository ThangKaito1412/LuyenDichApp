name: Android CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true # Sửa cảnh báo Node.js của GitHub

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout mã nguồn
      uses: actions/checkout@v4

    - name: Thiết lập JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Cấp quyền thực thi cho Gradle Wrapper
      run: chmod +x gradlew

    - name: Build file APK (Debug)
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }} # Truyền trực tiếp key vào môi trường build
      run: ./gradlew assembleDebug

    - name: Tải file APK lên GitHub Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: app-debug-apk
        path: app/build/outputs/apk/debug/app-debug.apk
